# 复制为 .env 后按需修改；docker compose 会自动读取
# 端口映射（宿主机:容器）
WEB_PORT=8000
SSH_PORT=10022

# 管理员账号（面板登录）
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# 容器内 root 密码（用于 SSH 终端登录）
ROOT_PASSWORD=changeme

# 可选：自定义 Python 基础镜像
# BASE_IMAGE=python:3.11-slim-bookworm
